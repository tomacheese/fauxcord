{
  // Production build config: excludes test files and test helpers from dist output
  // Type checking (pnpm run lint) uses tsconfig.json which includes tests
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "dist", "src/**/*.test.ts", "src/test-helpers.ts"]
}
