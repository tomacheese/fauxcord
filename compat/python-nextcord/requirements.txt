# Exactly one Discord fork must be installed in this environment: nextcord.
# Do NOT add discord.py or py-cord here -- all three PyPI distributions
# install the top-level import package under a different name (nextcord installs
# as `nextcord`, unlike discord.py/py-cord which both install as `discord`),
# but mixing REST-object-model libraries in one env is still avoided on
# principle, matching the isolation used for python-discordpy/ and python-pycord/.
#
# Version pin confidence: nextcord 2.6.0 was a real, current stable release as
# of this verifier's authoring; nextcord's release cadence is slower than
# discord.py/py-cord, so if this exact patch version has since been yanked or
# superseded, `pip install nextcord~=2.6` is a safe fallback.
nextcord==3.2.0
aiohttp
# nextcord's health_check module imports pkg_resources, which python:3.12-slim
# no longer provides out of the box. Newer setuptools (>=81) dropped pkg_resources
# entirely, so pin below that ceiling to get a version that still bundles it.
setuptools<83
